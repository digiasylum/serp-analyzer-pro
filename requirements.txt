# SERP V8 — All-in-One SEO & Marketing Intelligence + Gemini AI Layer
streamlit>=1.32.0
pandas
requests
beautifulsoup4
readability-lxml
python-slugify
tldextract
textstat
lxml
# Note: Gemini is called via raw `requests` (gemini_client.py) — no SDK needed.
# Note: `serpapi` package removed — unused; all SerpAPI calls go via `requests`.
